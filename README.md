
## Setup Instructions

1. Open `main.py` and set up your private key and address:

    ```python
    # Replace  variables with your own private key and address

    ```

2. Install the required dependencies using pip:

    ```bash
    pip install web3
    ```

3. (Optional) Add your own  addresses to the `wallets.txt` file. If you prefer not to modify the file, you can use the default addresses provided.

4. Run the script using the following command:

    ```bash
    python main.py
    ```

( script just send tags which you setted in main.py to addresses in wallets.py )
